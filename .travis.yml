language: android
android:
  components:
    - build-tools-23.0.3
  licenses:
    - 'android-sdk-license-.+'

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

script:
   - ./gradlew build
